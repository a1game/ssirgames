document.addEventListener('DOMContentLoaded', function() {
    // Get the element to be animated
    var an = document.getElementById('top');

    // Set the width to 10% after a short delay
    setTimeout(function() {
an.style.width = "35%"
    }, 5); // Adjust the delay as needed
});
